{
  "name": "backend-boilerplate",
  "version": "0.0.1",
  "scripts": {
    "build": "cd app && ./gradlew build",
    "start": "java -jar app/build/libs/spring-boot-boilerplate-0.0.1-SNAPSHOT-plain.jar",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^8.6.0"
  }
}
